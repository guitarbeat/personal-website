🎯 **What**
Added error path unit tests to verify the `catch` block of the `parseResponseJson` function in `src/server/notionContent.js`.

💡 **Why**
The complex error handling logic around JSON parsing needed verification to ensure that invalid JSON responses correctly trigger a fallback to `null` and subsequently fail gracefully with the expected `NOTION_API_ERROR` payload rather than crashing the server.

✅ **Verification**
Verified by running `pnpm test src/server/notionContent.test.js` which successfully passes all tests, including the new tests that simulate a `JSON.parse` exception. Also confirmed overall test suite continuity via `pnpm test`.

✨ **Result**
Increased test coverage for `notionContent.js`, specifically covering the error path in JSON parsing for both direct database queries and block children queries. This improves the reliability of the error-handling behavior.
